#pragma once
#include "Tarsashaz.h"
#include <stdio.h>
#include <fstream>
#include <sstream>
#include <iostream>

class Hazmester{
public:
    static void Karbantart(std::string path);
    static std::string Split(const std::string& sor,const int idx);
    
};
